import XCTest
import Firebase

final class HttpHeaderTestCases: XCTestCase {
    
    func testFirebaseTokenWithMissingToken() async throws {
        /*
        // Sign out the current user.
        try await Auth.auth().signOut()
        
        // Create an instance of the HttpHeader class.
        let httpHeader = HttpHeader()
        
        // Assert that the `firebaseToken()` method throws the expected error.
        XCTAssertThrowsError(try await httpHeader.firebaseToken(), String(describing: { error in
            XCTAssertEqual(error, FirebaseError.failToGetToken)
        }))*/
    }
    
}
